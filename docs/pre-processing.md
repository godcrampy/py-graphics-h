# Pre Processing

`pycparser` does not support all C features notably Directives and Comments.
`src/pre-processing.py` processes the file and removes comments and directives from the C file before generating the AST.
